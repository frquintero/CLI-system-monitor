# Contracts

This feature does not expose any APIs or interfaces that require formal contracts. The CLI System Monitor is a standalone command-line application that collects and displays system metrics directly.

No OpenAPI specifications, GraphQL schemas, or interface contracts are needed.